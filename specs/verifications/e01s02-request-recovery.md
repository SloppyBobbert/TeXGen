# e01s02 request recovery — accepted

Current result: body and generation repairs passed the final 244-test/31-browser gate and both independent reviews. See `e01s02-acceptance.md` and `e01s02-review-response.md`. The following entries record earlier checkpoints.

`b73c1e96c` reproduced three Dashboard failures with the original five tests retained: stale JSON replaced a newer account listing, logout retained account sheets, and a PDF body finishing after unmount created a download URL.

The parent added component/session-scoped controllers for listing, deletion, and download. Controllers remain registered through response-body processing. Cleanup aborts them on unmount or session change. Publication checks cancellation after JSON/blob reads, even when a mocked transport ignores abort. Listings carry their session identity so old account data is not rendered while the next effect starts. Download URL/anchor cleanup uses `finally`.

AuthProvider exposes its session version; normal token refresh keeps that version, while login/logout changes it. Thus refresh alone does not cancel current Dashboard work. Test providers without a version use their access token as the identity.

`b784801df` passed all 223 frontend tests, zero-warning lint, and the production build. `b713ab4d6` reproduced both LaTeX session-change failures: two failed, 47 intentionally filtered out. Each old PDF was published after logout/account switch. The parent now invalidates the existing operation epoch and cancels pending automatic compilation on session-version changes, without changing source/history. A third test verifies that normal token refresh within the same session does not invalidate a pending compile. `b5cb767f1` passed all 226 tests, strict lint, and the production build. `b576f4c48` reproduced two pending-save reconciliation failures: logout and account switch allowed an old response to replace the local source, title, and server ID. Normal token refresh passed; 30 unrelated cases were filtered out. The parent now advances the save epoch, aborts its controller, and clears pending-create bookkeeping on session-version changes without changing the local draft. `b73468794` passed all 229 tests, zero-warning lint, and the production build. Existing real-stack browser journeys are running as `ba1871b9a`. The follow-up in-memory history fix and complete gate passed as `b4da8e3cb`: 230 frontend tests, strict lint, build, and all three real-stack journeys. This covers the existing conflict/history/failure regressions plus the new App, LaTeX, and Dashboard session/body races. Tasks 2 and 3 pass their implementation checks. Independent review and story completion remain pending.
